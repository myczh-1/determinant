# Project Goal

## One-Sentence Summary

Determinant 将人类可审计的 AAL 程序确定性编译为可运行应用程序。

## Background

AI 可以把自然语言直接转换成代码，但业务决定容易隐藏在实现细节、默认值和模型推断中。Determinant 在自然语言与传统编程语言之间增加 AAL：用户审计 AAL 中的数据、流程、状态变化和失败条件，编译器负责稳定地生成实现。

## Core Value

- 用户审计“应用要做什么”，而不是大量程序组织细节；
- 未声明的业务决定不能被编译器静默补全；
- 用户确认 AAL 后，解析、检查、编译和测试不依赖 LLM；
- 同一份 AAL 在相同工具链下产生可复现结果。

## First-Version Goal

建立一条最小但真实可运行的链路：

```text
AAL 源文件
    ↓
确定性解析为 AST
    ↓
对象、流程和类型检查
    ↓
确定性生成 TypeScript
    ↓
Node.js 执行
```

端到端验收同时覆盖订单库存行为，以及一份完全由 AAL 声明行为、可真实运行的内存 HTTP CRUD 服务。

## Language Boundary

AAL 用户层只描述对象、流程和 HTTP 入口：

默认表层方言为英文，中文使用 `zh-CN` 方言；二者进入同一套 AST、检查器和生成器。

- `对象` 描述数据和状态；
- `流程` 描述输入、条件、计算、改变、执行、结果和失败；
- `HTTP 入口` 描述请求字段、目标流程、成功状态和失败映射；
- 字段关系使用 `的`；
- 流程组合使用 `执行 / 使用 / 得到`；
- 币种、单位、精度和失败条件必须显式表达。
- Host 与 Port 保持为 AAL 外部的运行配置。

编译器可以隐藏函数名、变量名、运行时对象和目标语言细节，但不能隐藏业务决定。

## Non-Goals

第一版不追求：

- 替代 TypeScript、Python 或 Go 等通用语言；
- 支持 UI 和视觉交互；
- 一次性覆盖数据库、持久化、分布式系统和全部并发语义；
- 让 AI 参与确认后的确定性编译链；
- 用默认值补全权限、重试、回滚、舍入或其他业务决定。

## Success Criteria

- 多对象、多流程的 AAL 程序可以解析、检查、编译和运行；
- 相同输入重复编译得到相同代码；
- 未定义名称、类型冲突、非法字段访问和不明确状态修改会被拒绝；
- 订单库存样例覆盖成功、库存不足、无效数量和库存状态变化；
- 用户只阅读 AAL 和诊断信息即可理解样例的主要业务语义。
- CRUD 样例覆盖真实 HTTP 创建、读取、修改、删除、输入失败和无 Binding 确定性生成。
