{
  "name": "determinant",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic compiler for the auditable application language AAL.",
  "type": "module",
  "bin": {
    "determinant": "bin/determinant.mjs"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.mjs",
    "compile:example": "npm run build && node bin/determinant.mjs examples/order.aal --binding bindings/order.binding.json --out generated/order.ts"
  }
}
